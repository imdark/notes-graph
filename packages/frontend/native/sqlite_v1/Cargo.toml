[package]
edition = "2024"
name    = "notesgraph_sqlite_v1"
publish = false
version = "0.0.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
anyhow = { workspace = true }
chrono = { workspace = true }
napi = { workspace = true }
napi-derive = { workspace = true }
notesgraph_schema = { path = "../schema" }
sqlx = { workspace = true, default-features = false, features = [
  "chrono",
  "macros",
  "migrate",
  "runtime-tokio",
  "sqlite",
] }
tokio = { workspace = true, features = ["full"] }

[build-dependencies]
dotenvy = { workspace = true }
napi-build = { workspace = true }
notesgraph_schema = { path = "../schema" }
sqlx = { workspace = true, default-features = false, features = [
  "chrono",
  "macros",
  "migrate",
  "runtime-tokio",
  "sqlite",
] }
tokio = { workspace = true, features = ["full"] }
