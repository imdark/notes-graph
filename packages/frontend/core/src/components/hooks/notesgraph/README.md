# NotesGraph Hooks

> This directory will be moved to `@notesgraph/worksapce/notesgraph/hooks` in the future.

Only put hooks in this directory if they are specific to NotesGraph, for example
if they are using the NotesGraph API, or if the `NotesGraphWorkspace` is required.
