query directoryAvailableOrgUnits($workspaceId: String!) {
  workspace(id: $workspaceId) {
    directoryAccount {
      id
      availableOrgUnits {
        externalId
        path
        name
      }
    }
  }
}
