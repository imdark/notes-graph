mutation assignDocToProject(
  $workspaceId: String!
  $docId: String!
  $projectId: String!
) {
  assignDocToProject(
    input: { workspaceId: $workspaceId, docId: $docId, projectId: $projectId }
  )
}
