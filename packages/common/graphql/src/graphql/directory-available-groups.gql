query directoryAvailableGroups($workspaceId: String!) {
  workspace(id: $workspaceId) {
    directoryAccount {
      id
      availableGroups {
        externalId
        email
        name
      }
    }
  }
}
