replicaCount: 1
image:
  repository: ghcr.io/notesgraph/notesgraph
  pullPolicy: IfNotPresent
  tag: ''

imagePullSecrets: []
nameOverride: ''
fullnameOverride: ''
# map to NODE_ENV environment variable
env: 'production'
nodeOptions: '--max-old-space-size=3072'
app:
  # NOTESGRAPH_SERVER_PORT
  port: 3010
  # NOTESGRAPH_SERVER_SUB_PATH
  path: ''
  # NOTESGRAPH_SERVER_HOST
  host: '0.0.0.0'
  https: true
serviceAccount:
  create: true
  annotations: {}
  name: 'notesgraph-front'

podAnnotations: {}

podSecurityContext:
  fsGroup: 2000

resources:
  limits:
    cpu: '1'
    memory: 6Gi
  requests:
    cpu: '1'
    memory: 4Gi

probe:
  initialDelaySeconds: 20
  timeoutSeconds: 5
  periodSeconds: 10
  failureThreshold: 6
  successThreshold: 1
  liveness:
    initialDelaySeconds: 60
    failureThreshold: 12
  readiness: {}

services:
  sync:
    name: notesgraph-sync
    type: ClusterIP
    port: 3010
    annotations:
      cloud.google.com/backend-config: '{"default": "notesgraph-api-backendconfig"}'
  renderer:
    name: notesgraph-renderer
    type: ClusterIP
    port: 3000
    annotations:
      cloud.google.com/backend-config: '{"default": "notesgraph-api-backendconfig"}'
  web:
    name: notesgraph-web
    type: ClusterIP
    port: 8080
    annotations: {}
  doc:
    type: ClusterIP
    annotations: {}

nodeSelector: {}
tolerations: []
affinity: {}
